# ============================================
# OpenAI API Configuration
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your_api_key_here

# OpenAI API base URL (usually no need to change)
OPENAI_BASE_URL=https://api.openai.com/v1

# ============================================
# Server Configuration
# ============================================
# Host to bind to (0.0.0.0 = all interfaces)
HOST=0.0.0.0

# Port to listen on
PORT=8000

# ============================================
# LLM Configuration
# ============================================
# Which OpenAI model to use
# Options: gpt-3.5-turbo, gpt-4, gpt-4-turbo
LLM_MODEL=gpt-3.5-turbo

# Maximum tokens in LLM response (lower = cheaper)
LLM_MAX_TOKENS=150

# Creativity level (0.0 = deterministic, 1.0 = creative)
LLM_TEMPERATURE=0.7

# ============================================
# TTS Configuration
# ============================================
# TTS model (tts-1 is faster, tts-1-hd is higher quality)
TTS_MODEL=tts-1

# Voice to use
# Options: alloy, echo, fable, onyx, nova, shimmer
TTS_VOICE=alloy

# Audio format
# Options: mp3, opus, aac, flac
TTS_RESPONSE_FORMAT=mp3

# ============================================
# Timeout Configuration
# ============================================
# Timeout for OpenAI API calls (in seconds)
# Prevents hanging if OpenAI is slow
REQUEST_TIMEOUT=30
